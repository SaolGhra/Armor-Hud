modLoader = "javafml"
loaderVersion = "[1,)"
license = "MIT"
issueTrackerURL = "https://github.com/SaolGhra/Armor-Hud/issues"

[[mods]]
modId = "${id}"
version = "${version}"
displayName = "${name}"
authors = "SaolGhra"
description = '''Displays the durability of your armor in a simple and clean way.'''
logoFile = "armor_hud_logo.png"
displayURL = "https://modrinth.com/mod/armor-hud"

[[dependencies.${id}]]
modId = "neoforge"
${required}
versionRange = "${neoforge}"
ordering = "NONE"
side = "CLIENT"

[[dependencies.${id}]]
modId = "minecraft"
${required}
versionRange = "${minecraft}"
ordering = "NONE"
side = "CLIENT"

# Verification-only mixin, absent from release builds (the `verify` const guards this block and the
# mixin class both). Registers the framebuffer-screenshot mixin used by the headless HUD check.
#? if verify {
#[[mixins]]
#config = "armor_hud-verify.mixins.json"
#?}
