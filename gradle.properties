# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx3G
org.gradle.caching=true
org.gradle.configureondemand=true

# Mod properties
mod.version=3.5.0
mod.group=com.saolghra
mod.id=armor_hud
mod.name=Armor HUD

# Metadata (overridden per-version in versions/<mc>/gradle.properties)
mod.mc_dep_fabric=[VERSIONED]
mod.mc_dep_forgelike=[VERSIONED]
# Human-readable title for the release, e.g. '1.21.1'
mod.mc_title=[VERSIONED]
# Space-separated versions for Modrinth/CurseForge publishing
mod.mc_targets=[VERSIONED]

# Shared dependency versions (loader/version-specific ones live in versions/<mc>/gradle.properties)
deps.mixin_extras=0.4.1

# Per-version dependency versions (placeholders; real values in versions/<mc>/gradle.properties)
deps.fabric_loader=[VERSIONED]
deps.fabric_api=[VERSIONED]
deps.architectury_api=[VERSIONED]
deps.neoforge_loader=[VERSIONED]
deps.forge_loader=[VERSIONED]
deps.modmenu=[VERSIONED]
